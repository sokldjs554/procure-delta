/* eslint-disable react-hooks/exhaustive-deps -- cleanup invalidates the active request generation */
"use client";
import { useEffect, useRef, useState } from "react";
import { getWatchlist, type Opportunity } from "../../../lib/api";
import { appendUniquePage } from "../../../lib/product";
import { OpportunityCard } from "../../../components/opportunities/opportunity-card";
export default function Watchlist(){const[items,setItems]=useState<Opportunity[]|null>(null);const[cursor,setCursor]=useState<string|null>(null);const[more,setMore]=useState(false);const[error,setError]=useState("");const generation=useRef(0);useEffect(()=>{const request=++generation.current;getWatchlist().then(page=>{if(request===generation.current){setItems(page.items);setCursor(page.next_cursor)}}).catch((e:Error)=>{if(request===generation.current)setError(e.message)});return()=>{generation.current++}},[]);async function loadMore(){if(!cursor||more)return;const request=generation.current;setMore(true);try{const page=await getWatchlist(cursor);if(request!==generation.current)return;setItems(current=>appendUniquePage(current??[],page.items));setCursor(page.next_cursor)}catch(e){if(request===generation.current)setError((e as Error).message)}finally{if(request===generation.current)setMore(false)}}return <main className="product"><header className="page-head"><div><p className="eyebrow">WATCHLIST</p><h1>관심 공고</h1><p>변경과 결과를 계속 추적하도록 저장한 공고입니다.</p></div></header>{error&&!items?<div className="state error">{error}</div>:items===null?<div className="state"><span className="spinner"/>불러오는 중</div>:items.length?<><section className="cards">{items.map(item=><OpportunityCard item={item} key={item.id}/>)}</section>{error&&<p className="load-error" role="alert">{error}</p>}{cursor&&<div className="load-more"><button disabled={more} onClick={loadMore}>{more?"불러오는 중…":"관심 공고 더 보기"}</button></div>}</>:<div className="state"><h2>관심 공고가 아직 없습니다</h2><p>공고 상세에서 ‘관심 공고로 추적’을 선택하세요.</p></div>}</main>}
